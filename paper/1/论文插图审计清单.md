# 论文插图审计清单

本文件用于给 `paper/rust_waf_thesis.html` 中的图片、SVG、图题和截图做持续盘点。

目标不是一次性“修完”，而是让后续每次改论文时都能快速判断：

- 哪些图仍然准确
- 哪些图只是图题要改
- 哪些图需要重画 SVG
- 哪些图需要重新截图
- 哪些新页面已经写进正文，但还没有配图

---

## 2026-04-14 审计结论

### 总体判断

- 当前论文实际使用 6 张图，其中 3 张是 SVG，4 张是页面截图，另有 1 张思维导图以 `pre` 文本块形式出现。
- 3 张 SVG 里，`图4-2` 基本准确；`图3-1` 和 `图4-1` 可以继续用，但建议做一次小修。
- 4 张页面截图里，`图4-3`、`图4-5`、`图4-6` 已明显落后于当前前端结构；`图4-4` 仍有参考价值，但也不算最新。
- 当前正文已经大量描述“系统设置页中的自动化接管总览”“L4/L7 独立运行项与兼容层入口”“动作中心”等新结构，但论文没有任何对应截图，这是后续最需要补的空缺。

---

## 当前图像资产盘点

### 已在论文中使用

| 图号 | 资源 | 类型 | 当前判断 |
|---|---|---|---|
| 图3-1 | `paper/canva-assets/01-system-architecture.svg` | SVG | 基本准确，建议小修 |
| 图3-2 | HTML 内 `pre` 思维导图 | 文字图 | 已偏旧，建议更新 |
| 图4-1 | `paper/canva-assets/02-request-flow.svg` | SVG | 基本准确，建议小修 |
| 图4-2 | `paper/canva-assets/03-browser-fingerprint-flow.svg` | SVG | 准确，可保留 |
| 图4-3 | HTML 内嵌首页截图 | PNG | 已过时，建议重截 |
| 图4-4 | HTML 内嵌规则页截图 | PNG | 部分过时，建议替换或重截 |
| 图4-5 | HTML 内嵌站点页截图 | PNG | 已过时，建议重截 |
| 图4-6 | HTML 内嵌 SafeLine 页截图 | PNG | 已过时，建议重截或改题材 |

### 未在论文中使用

| 资源 | 类型 | 备注 |
|---|---|---|
| `paper/cc-attack-sequence.svg` | SVG | 目前未引用，可作为“CC/联动链路补充图”的候选素材 |

---

## 分图审计

### 图3-1 系统总体架构示意

资源：`paper/canva-assets/01-system-architecture.svg`

当前优点：

- 五层结构仍然和论文正文一致
- 管理控制层已经写到“动作中心与规则”“L4 / L7 看板”“SafeLine 联动”
- 持久化层已经覆盖 `security_events`、站点 / 证书 / 同步链路

当前问题：

- 图中“Vue 控制台”下面的分项还偏概括，没有显式体现最新的重要结构：
  - 系统设置页集中展示自适应防护状态
  - L4 / L7 页面更偏运行观测页
  - 独立运行项与兼容层入口分层
- “L4 / L7 看板”这个说法不算错，但现在更准确的说法应该是：
  - L4 / L7 管理页
  - 系统设置页中的自动化接管总览

建议动作：

- 保留此图
- 小修管理控制层文字

建议改法：

- 把 `站点 / 证书 / 设置` 细化成 `系统设置 / 站点 / 证书`
- 把 `L4 / L7 看板` 改成 `L4 / L7 管理`
- 在管理层说明里补一句“自动化接管项与独立运行项分层展示”

---

### 图3-2 系统思维导图

资源：HTML 内 `pre` 文本块

当前问题：

- 仍然把控制层写成比较早期的概括：
  - 管理API
  - Vue控制台
  - 指标看板
  - SafeLine联动
- 没有体现后续已经稳定进入论文的内容：
  - 动作中心
  - 证书管理
  - 系统设置
  - 浏览器指纹诱导链路
  - L4/L7 兼容层归档入口

建议动作：

- 建议更新
- 二选一：
  - 继续保留 `pre` 文本图，但把节点补新
  - 或者直接改成一个真正的 SVG 思维导图

我的建议：

- 如果时间有限，就继续保留 `pre` 文本图，只改节点内容
- 如果准备答辩材料，最好把它升级成 SVG，视觉会统一很多

---

### 图4-1 请求处理流程思维导图

资源：`paper/canva-assets/02-request-flow.svg`

当前优点：

- 主链路仍然成立：请求进入系统 -> PacketInfo -> L4 -> 协议识别 -> 统一请求抽象 + L7 -> 网关代理
- “L4 先粗筛，L7 再精判”的论文主旨表达清楚

当前问题：

- 对当前实现来说，这张图略微偏简化：
  - 没有显式画出安全事件落库
  - 没有体现 SafeLine 接管 / 上游治理回路
  - 没有体现浏览器指纹上报属于特殊支路

建议动作：

- 保留此图
- 做一次小修即可，不必重画

建议补充：

- 在 `L7 检测` 或 `网关代理` 之后增加一个“事件审计 / SQLite 落库”分支
- 可在旁注里补一个“SafeLine 接管 / 联动反馈”侧支

---

### 图4-2 浏览器指纹诱导链路图

资源：`paper/canva-assets/03-browser-fingerprint-flow.svg`

当前判断：

- 这是目前最准确的一张图
- 图中文字和正文描述基本一致
- 已经体现：
  - respond 模板命中
  - 浏览器侧采集
  - 高数题干扰层
  - `/.well-known/waf/browser-fingerprint-report`
  - `security_events`
  - `provider=browser_fingerprint`

建议动作：

- 直接保留
- 当前无需修改

---

### 图4-3 管理控制台首页运行界面

资源：HTML 内嵌截图

当前截图特征：

- 左侧导航还是旧结构：
  - `站点管理`
  - `规则中心`
  - `动作中心`
  - `L4管理`
  - `L7管理`
  - `雷池联动`
  - `系统设置`
- 当前实际代码中的导航已经改成分组：
  - `站点管理` 组：站点管理 / 证书管理
  - `防御策略` 组：规则中心 / 动作中心
  - 无单独顶层“雷池联动”
- 首页内容还是旧版卡片，不足以支撑现在正文中对“系统设置页中的自动化接管总览”的描述

结论：

- 明显过时

建议动作：

- 重截
- 最好不要再用“总览首页”作为唯一控制台代表图

更好的替代方案：

- 新截“系统设置页”而不是首页
- 因为当前论文真正强调的是：
  - 自适应防护状态
  - 自动化接管项与独立运行项分层
  - L4/L7 兼容层入口

---

### 图4-4 规则管理页面界面

资源：HTML 内嵌截图

当前截图特征：

- 页面展示了规则列表和规则模板插件上传
- 从历史角度看并不完全错误

当前问题：

- 现在论文已经明显把“动作中心”写成重要能力
- 但这张图没有体现动作中心、模板预览、场景化示例
- 因此它只能证明“规则中心”，不能证明现在正文里新增的动作模板组织方式

结论：

- 部分过时

建议动作：

- 二选一：
  - 重截规则中心最新界面
  - 或直接把这张图换成“动作中心页面截图”

我的建议：

- 更推荐换成“动作中心页面截图”
- 因为动作中心是论文现在的差异化亮点，比单纯规则列表更值得展示

---

### 图4-5 站点管理页面界面

资源：HTML 内嵌截图

当前截图特征：

- 页面里仍把本地站点、远端站点和大量站点卡片堆在单页上展示

当前代码变化：

- 当前 `AdminSitesPage.vue` 更偏“摘要 + 表格 + 同步弹窗”
- 证书管理已经拆到独立页面
- 远端站点同步通过对话框完成，而不是旧式单页堆叠

结论：

- 明显过时

建议动作：

- 重截当前站点管理页
- 如果篇幅允许，可以再加一张“站点同步弹窗”截图

图题建议：

- 旧题：`图4-5 站点管理页面界面`
- 可保留，但图内内容必须换成当前摘要表格式页面

---

### 图4-6 SafeLine联动页面界面

资源：HTML 内嵌截图

当前截图特征：

- 左侧导航仍有独立顶层 `雷池联动`
- 页面内容是较早期的“接入概况 + 联动操作 + 站点映射管理”

当前代码变化：

- 导航结构已调整，不再保留旧式顶层入口
- SafeLine 相关能力已经分散进多个位置：
  - 系统设置页的联动配置与测试
  - 站点管理页的远端同步
  - 证书管理页的雷池证书同步
  - 事件页的来源筛选
  - L7 中的 SafeLine 响应接管

结论：

- 明显过时，而且“单页即可代表全部联动能力”的叙事也已经不准确

建议动作：

- 不建议简单重截同一旧页面
- 更建议改成新的图题和新截图题材

推荐替代方案：

1. 用“系统设置页联动配置区截图”替换
2. 或用“证书管理页的雷池证书同步”替换
3. 或保留 SafeLine 主题，但把图题改成更准确的：
   - `图4-6 系统联动与同步能力界面`

---

## 当前最缺的配图

这些内容已经在正文里写得比较重，但目前没有对应截图：

### 1. 系统设置页

理由：

- 正文已强调“系统设置页集中展示自动化接管状态”
- 这是最新 UI 架构变化的核心证据

优先级：

- 最高

---

### 2. L4 管理页

理由：

- 当前代码中它已经更像“观测页”
- 包含行为分桶、运行时洞察、架构说明等可展示内容

优先级：

- 高

---

### 3. L7 管理页

理由：

- 当前代码中它也已经是“观测页 + 自动控制器生效策略展示”
- 与论文“L7 独立运行项 / 兼容层归档”的文字对应紧密

优先级：

- 高

---

### 4. 动作中心页

理由：

- 论文当前已经把动作中心写成特色能力
- 但现在没有任何截图证明它的存在

优先级：

- 高

---

## 推荐的下一轮插图改造顺序

如果只做最小必要更新，建议顺序如下：

1. 重截系统设置页，替换图4-3
2. 重截动作中心页，替换图4-4
3. 重截当前站点管理页，替换图4-5
4. 把图4-6改成“联动与同步能力界面”，重新选题材截图
5. 小修 `图3-1` 架构 SVG
6. 小修 `图4-1` 请求流程 SVG
7. 更新 `图3-2` 思维导图

如果准备答辩，建议再做两件事：

1. 把 `paper/cc-attack-sequence.svg` 评估后接入正文或附录
2. 补一张 L4 或 L7 管理页截图，证明“观测页 + 兼容层归档”的新结构

---

## 我对下次执行的建议

下次如果你让我继续完善论文，最合适的说法是：

`先看 paper/论文插图审计清单.md、paper/论文更新日志.md 和 paper/thesis-update-skill/SKILL.md，再根据这次代码改动优先更新受影响的截图和 SVG，然后同步 paper/rust_waf_thesis.html。`

这样我会先处理图，再处理字，不会再只更新正文。
